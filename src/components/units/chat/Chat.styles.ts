import styled from "@emotion/styled";
import { MessageFilled } from "@ant-design/icons";

export const ChatButton = styled.button`
  background-color: transparent;
  color: var(--gray);
  font-size: 20px;
  border: none;
  cursor: pointer;
`;

export const ChatIcon = styled(MessageFilled)``;
