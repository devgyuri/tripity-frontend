import styled from "@emotion/styled";
import { DownOutlined, UpOutlined } from "@ant-design/icons";

export const ListUpIcon = styled(UpOutlined)`
  color: var(--gray);
`;

export const ListDownIcon = styled(DownOutlined)`
  color: var(--gray);
`;
