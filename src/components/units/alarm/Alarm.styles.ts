import styled from "@emotion/styled";
import { BellFilled } from "@ant-design/icons";

export const AlarmButton = styled.button`
  margin-right: 15px;
  background-color: transparent;
  color: var(--gray);
  font-size: 24px;
  border: none;
  cursor: pointer;
`;

export const AlarmIcon = styled(BellFilled)``;
